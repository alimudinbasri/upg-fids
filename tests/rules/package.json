{
  "name": "upg-fids-rules-tests",
  "private": true,
  "description": "Firestore security rules tests. Runs against the local emulator only — never touches the upg-fids project.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-upg-fids --config firebase.test.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.11.0"
  }
}
